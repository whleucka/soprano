# APPLICATION
APP_NAME=Echo
APP_VERSION=v0.0.1
APP_URL=http://localhost
APP_DEBUG=true                    # Set to false in production
APP_TIMEZONE=UTC
APP_KEY=

# DOCKER
BUILD_MODE=dev
# Hostname used by Traefik in dev (http://<DEV_HOSTNAME>.localhost). Required by docker-compose.dev.yml.
DEV_HOSTNAME=soprano
# Public hostname used by Traefik + Let's Encrypt in prod. Required by docker-compose.prod.yml.
PROD_HOSTNAME=soprano.example.com
# Host path to your music library. Mounted into php (rw) and nginx (ro) at /music.
# Required by docker-compose.local.yml (dev) and docker-compose.prod.yml.
MUSIC_DIR=/path/to/your/Music

# BENCHMARKS
BENCHMARKS_ENABLED=false

# SECURITY
AUTH_REGISTER_ENABLED=true

# DATABASE
DB_DRIVER=mariadb
DB_NAME=echo
DB_USERNAME=
DB_ROOT_PASSWORD=
DB_HOST=db                        # Use "db" for Docker, "localhost" for host
DB_PASSWORD=
DB_PORT=3306
DB_CHARSET=utf8mb4

# MAIL
MAIL_HOST=localhost
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME=Echo

# REDIS
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=                   # Set a password in production
REDIS_DATABASE=0
REDIS_PREFIX=echo:
REDIS_CACHE_DB=1
REDIS_SESSION_DB=2
REDIS_RATE_LIMIT_DB=3
REDIS_QUEUE_DB=4

# SESSION
SESSION_DRIVER=file               # Use "redis" in production

# CACHE
CACHE_DRIVER=file                 # Use "redis" in production

# GEOIP (MaxMind GeoLite2)
# Sign up free at https://www.maxmind.com/en/geolite2/signup
MAXMIND_ACCOUNT_ID=
MAXMIND_LICENSE_KEY=

# TRUSTED PROXIES
TRUSTED_PROXIES=

# COMPANY
COMPANY_NAME=
COMPANY_CONTACT_EMAIL=
COMPANY_CONTACT_ADDRESS=
COMPANY_CONTACT_PHONE=
COMPANY_COPYRIGHT_EMAIL=
COMPANY_SUPPORT_EMAIL=
