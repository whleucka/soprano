<div class="col-12 col-lg-{{ widget.width }} mb-4" id="widget-{{ widget.id }}"
  {% if widget.refresh_interval > 0 %}
  hx-get="{{ uri('dashboard.widgets.render', {id: widget.id}) }}"
  hx-trigger="every {{ widget.refresh_interval }}s"
  hx-swap="outerHTML"
  {% endif %}>
  <div class="card shad border-0 h-100">
    <div class="card-header border-0 d-flex justify-content-between align-items-center">
      <span><i class="bi bi-{{ widget.icon }} me-2"></i>{{ widget.title }}</span>
      <button class="btn btn-sm btn-outline-secondary"
        hx-get="{{ uri('dashboard.widgets.render', {id: widget.id}) }}"
        hx-swap="outerHTML"
        hx-target="#widget-{{ widget.id }}">
        <i class="bi bi-arrow-clockwise"></i>
      </button>
    </div>
    <div class="card-body">
      {% if data.tracks|length > 0 %}
      <div class="table-responsive" style="max-height: 260px;">
        <table class="table table-sm table-hover mb-0 align-middle">
          <thead class="table-light">
            <tr>
              <th style="width: 2rem;">#</th>
              <th>Title</th>
              <th>Artist</th>
              <th class="text-end">Plays</th>
            </tr>
          </thead>
          <tbody>
            {% for track in data.tracks %}
            <tr>
              <td class="text-muted">{{ loop.index }}</td>
              <td class="text-truncate" style="max-width: 160px;">{{ track.title ?: 'Unknown' }}</td>
              <td class="text-truncate text-muted" style="max-width: 140px;">{{ track.artist ?: 'Unknown' }}</td>
              <td class="text-end"><span class="badge bg-secondary bg-opacity-75">{{ track.plays|number_format }}</span></td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
      {% else %}
      <p class="text-muted text-center mb-0">No plays recorded yet</p>
      {% endif %}
    </div>
    <div class="card-footer border-0">
      <a href="/tracks" hx-boost="true" hx-target="#module" hx-select="#module" hx-swap="outerHTML" hx-push-url="true" class="text-muted small">
        <i class="bi bi-arrow-right me-1"></i>View all tracks
      </a>
    </div>
  </div>
</div>
