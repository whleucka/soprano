{% if tracks %}
  {% for track in tracks %}
    {% embed 'components/album-cont.html.twig' with {
      track: track,
      menu_id: 'ra',
      info: track.added_ago ? [{ icon: 'bi-plus-lg', text: 'Added ' ~ track.added_ago }] : [],
    } %}
    {% block badge %}
      {# Not every album has a year — without the guard the badge renders as a
         bare black pill on the cover. #}
      {% if track.year %}
        <span class="year-badge" title="{{ track.year }}">{{ track.year }}</span>
      {% endif %}
    {% endblock %}
    {% block extra %}
      <span title="{{ track.album }}" class="title truncate fw-bold mt-1">{{ track.album }}</span>
      <span title="{{ track.artist }}" class="artist small truncate"><i class="bi bi-person pe-1"></i> {{ track.artist }}</span>
    {% endblock %}
    {% endembed %}
  {% endfor %}
{% else %}
  <p class="lead"><i class="bi bi-info pe-1 text-warning"></i> No music has been added recently</p>
{% endif %}
