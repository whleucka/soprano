{% extends "layout/app.html.twig" %}

{% block title %}Soprano - Home{% endblock %}

{% block view %}
<section id="home" class="h-100">
  <div class="hero d-flex align-items-center p-3">
    <h1>Home</h1>
  </div>
  <div class="p-3">
    <h2>Recently Added Music</h2>
    <section id="recently-added">
      <div class="d-flex justify-content-start placeholder-glow" hx-get="{{ uri("home.recently-added") }}" hx-trigger="load">
        <div class="placeholder"></div>
        <div class="placeholder"></div>
        <div class="placeholder"></div>
      </div>
    </section>
  </div>
</section>
{% endblock %}
